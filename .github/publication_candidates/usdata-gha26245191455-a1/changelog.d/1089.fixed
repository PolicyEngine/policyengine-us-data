Self-heal publication candidate generation when the finalized Hugging Face data release is ahead of the local package version, and remove PolicyEngine US PyPI freshness gates from main-push and pipeline runs.
