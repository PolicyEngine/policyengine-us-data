name: Manual tests

on:
  workflow_dispatch:
    inputs:
      test_lite:
        description: 'Run in lite mode'
        required: true
        default: true
        type: boolean

jobs:
  test:
    uses: ./.github/workflows/pr_code_changes.yaml
    with:
      TEST_LITE: ${{ inputs.test_lite }}
    secrets: inherit
