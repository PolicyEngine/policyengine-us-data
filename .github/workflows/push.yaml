name: Push to main

on:
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: pip install ruff>=0.9.0
      - run: ruff format --check .

  publication-context:
    name: Publication context
    runs-on: ubuntu-latest
    if: github.event.head_commit.message != 'Update package version'
    outputs:
      publication_id: ${{ steps.publication-context.outputs.publication_id }}
      github_run_url: ${{ steps.publication-context.outputs.github_run_url }}
    env:
      MODAL_ENVIRONMENT: main
      US_DATA_MODAL_APP_PREFIX: policyengine-us-data-pub
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - name: Resolve publication context
        id: publication-context
        run: python .github/scripts/resolve_publication_context.py

  # ── Dataset build ───────────────────────────────────────────
  build-datasets:
    name: Build datasets
    runs-on: ubuntu-latest
    needs:
      - lint
      - publication-context
    if: github.event.head_commit.message != 'Update package version'
    env:
      MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
      MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
      MODAL_ENVIRONMENT: main
      HUGGING_FACE_TOKEN: ${{ secrets.HUGGING_FACE_TOKEN }}
      US_DATA_PUBLICATION_ID: ${{ needs.publication-context.outputs.publication_id }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - run: pip install modal
      - name: Resolve publication context
        id: publication-context
        env:
          US_DATA_MODAL_APP_PREFIX: policyengine-us-data-build
        run: python .github/scripts/resolve_publication_context.py
      - name: Build datasets on Modal
        run: |
          modal run --env="${MODAL_ENVIRONMENT}" modal_app/data_build.py \
            --upload \
            --branch=${{ github.ref_name }} \
            --run-id="${PUBLICATION_ID}"

  # ── Documentation ──────────────────────────────────────────
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    if: github.event.head_commit.message != 'Update package version'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
      - uses: astral-sh/setup-uv@v8.1.0
      - run: uv sync --dev
      - name: Build documentation
        run: uv run make documentation
        env:
          BASE_URL: /policyengine-us-data
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs/_build/html
          clean: true

  # ── Versioning (bump + changelog on non-version-bump pushes) ──
  versioning:
    name: Versioning
    runs-on: ubuntu-latest
    needs: publication-context
    if: github.event.head_commit.message != 'Update package version'
    outputs:
      version_sha: ${{ steps.version-commit.outputs.sha }}
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
      - uses: actions/checkout@v6
        with:
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@v8.1.0
      - run: pip install towncrier
      - name: Bump version and build changelog
        run: |
          python .github/bump_version.py
          towncrier build --yes --version "$(python .github/fetch_version.py)"
      - name: Update lockfile
        run: uv lock
      - name: Update changelog
        uses: EndBug/add-and-commit@v10
        with:
          add: "."
          message: Update package version
      - name: Capture version commit
        id: version-commit
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  # ── Full publication pipeline ───────────────────────────────
  launch-pipeline:
    name: Launch publication pipeline
    runs-on: ubuntu-latest
    needs:
      - publication-context
      - build-datasets
      - versioning
    if: github.event.head_commit.message != 'Update package version'
    permissions:
      actions: write
      contents: read
    steps:
      - name: Dispatch pipeline workflow
        env:
          GH_TOKEN: ${{ github.token }}
          PUBLICATION_ID: ${{ needs.publication-context.outputs.publication_id }}
          SOURCE_SHA: ${{ needs.versioning.outputs.version_sha }}
        run: bash .github/scripts/dispatch_publication_pipeline.sh

  # ── PyPI publish (version bump commits only) ────────────────
  publish:
    runs-on: ubuntu-latest
    needs: lint
    if: github.event.head_commit.message == 'Update package version'
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@v8.1.0
      - run: uv sync --dev
      - run: uv run python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI }}
          skip-existing: true
