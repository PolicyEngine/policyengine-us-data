Split calibration checkpoint signature validation into fatal structural
mismatches and soft hyperparameter mismatches, letting callers tune
`lambda_l0`, `beta`, `lambda_l2`, and `learning_rate` across resume phases.
Replace percentile-based top selection in `create_stratified_cps` with
per-bracket caps on the high-AGI tail to keep PUF templates from piling up
above $10M. Add `income_tax` national and state SOI targets, drop the
unachievable JCT `deductible_mortgage_interest` target, and preserve
positive mortgage interest inputs through structural conversion.
Retune the national Modal calibration to `lambda_l0=2e-2` at 1000 epochs.
Add a memory-bounded chunked matrix builder (`--chunked-matrix`) that
streams calibration columns in clone-household chunks instead of
materializing the full geography-cartesian product upfront, with a
shared `entity_clone` module for household-subset materialization.
Add `analyze_target_consistency` diagnostic that flags cross-level and
AGI-bucket-coverage inconsistencies in calibration target inputs, and
use it to fix a definitional mismatch where the IRS workbook override
for `total_self_employment_income`, `tax_unit_partnership_s_corp_income`,
and `net_capital_gains` was writing gross profits into slots the
district-level targets treat as net-of-losses (+40.7% / +26.1% / +3.1%
overstatement at 2023 values).
