- bump: patch
  changes:
    changed:
      - Simplified CI test matrix to only test on Python 3.12 and Ubuntu
      - Updated scipy dependency from <1.13 to >=1.15.3 for better compatibility
      - Updated pandas dependency from >=2.3.0 to >=2.3.1
      - Updated statsmodels dependency from >=0.14.0 to >=0.14.5
      - Added lower bounds to dependencies that were missing them