"""Internal package for the incremental local H5 migration.

Themed subpackages under ``requests``, ``inputs``, ``validation``,
``results``, and ``partitioning`` are the intended import surfaces.
This package deliberately does not re-export their contents.
"""
